{
  "name": "@astilba/site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build && node scripts/verify-built-site.mjs",
    "build:production": "cross-env ASTILBA_SITE=https://astilba.com pnpm build",
    "check": "astro check && tsc --noEmit",
    "deploy": "pnpm build:production && wrangler deploy",
    "deploy:dry-run": "pnpm build:production && wrangler deploy --dry-run",
    "dev": "astro dev",
    "preview": "astro preview",
    "smoke:production": "node scripts/smoke-production.mjs",
    "test": "vitest run",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "@astilba/tokens": "0.1.2",
    "@astilba/ui": "0.1.2",
    "@astrojs/react": "6.0.1",
    "@fontsource-variable/geist": "5.3.0",
    "@fontsource-variable/inter": "5.3.0",
    "@fontsource-variable/jetbrains-mono": "5.3.0",
    "astro": "7.1.3",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.62.0",
    "ajv": "8.20.0",
    "create-astilba": "0.3.0",
    "cross-env": "10.1.0",
    "typescript": "6.0.3",
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
